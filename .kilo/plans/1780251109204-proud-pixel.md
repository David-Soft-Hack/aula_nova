# Plan: Mejoras en AddStudentDialog y馃�oincrementable de c贸digo

## Objetivos
1. Indicar visualmente los campos obligatorios en "Datos Personales".
2. Reemplazar el ingreso manual de `codigo` por autocompletado/incremento autom谩tico basado en el 煤ltimo valor guardado.
3. Agregar marcas de agua (hints) informativas en todos los campos del formulario.

---

## Cambios en `lib/views/students/widgets/add_student_dialog.dart`
- Eliminar el `TextEditingController` de `codigo`.
- En el `initState`, obtener el pr贸ximo c贸digo disponible (string) desde el controlador y mostrarlo en un `TextFormField` de solo lectura o deshabilitado dentro de la vista de paso 1.
- Ajustar validaciones para no exigir ingreso manual de `codigo`.
- Limpiar dispose (eliminar controlador de c贸digo si aplica).

---

## Cambios en `lib/controllers/student_controller.dart`
- Agregar m茅todo: `Future<String?> getNextStudentCode() async` que:
  - Consulte la tabla `Students` filtrando por `carrera` (la carrera seleccionada en el paso 2, si existe).
  - Obtenga el c贸digo num茅rico m谩ximo del grupo correspondiente (`grupo` tambi茅n).
  - Genere el pr贸ximo c贸digo formateado (ej: `TE-2024-001` -> `TE-2024-002`).
  - Si no hay alumnos para esa carrera/grupo, devuelva el primer c贸digo sugerido (ej: `TE-2024-001`).
- Ajustar `existsStudentByCodigo` si es necesario (debe mantenerse como respaldo).
- Considerar que el c贸digo puede tener un formato conocido (ej: letra carrera + a帽o + secuencia).

---

## Cambios en `lib/database/tables.dart`
- Revisar si se requiere un 脥ndice compuesto en `Students` para `(carrera, grupo)` para mejorar performance en la consulta de pr贸ximo c贸digo.
- Opcionalmente, se puede agregar una columna auxiliar `codigoNumerico` para facilitar el incremento, aunque se prefiere parsear el c贸digo existente.

---

## Estado actual de BD
- El campo `email` en `Students` ya es nullable (`text().nullable()()`), por lo que **no requiere migración**; ya es opcional.

## Cambios visuales en campos (widgets reutilizables)
- Marcar con `*` o color los labels de campos obligatorios: C贸digo, Nombres, Apellidos, Tel茅fono.
- Agregar `hintText` en cada campo:
  - C贸digo: "Se asignar谩 autom谩ticamente" (si es autocompletado).
  - Nombres: "Ej: Juan Carlos".
  - Apellidos: "Ej: P茅rez L贸pez".
  - Email: "Ej: juan@correo.com".
  - Tel茅fono: "Ej: +51 987654321".
- El campo de fecha de ingreso ya es claro, se puede reforzar con hint.

---

## Pasos de implementaci贸n
1. Leer formato actual de c贸digos en BD y definir regla de generaci贸n.
2. Implementar `getNextStudentCode` en `StudentController` consultando por carrera/grupo.
3. Modificar `AddStudentDialog` para usar el c贸digo autogenerado.
4. Actualizar `personal_data_step.dart` y `class_group_step.dart` para reflejar obligatoriedad e hints.
5. Ejecutar an谩lisis est谩tico (`flutter analyze`) y formato (`dart format`).
6. Validar flujo completo en la app y confirmar que no se duplican c贸digos.

---

## Consideraciones
- El autocompletado debe realizarse **despu茅s** de seleccionar carrera y grupo en paso 2, mostr谩ndolo en paso 1 o recargando el paso 1 con el valor.
- Si el usuario cambia carrera/grupo, se debe recalcular el c贸digo sugerido.
- Se mantiene el campo `codigo` como `text().unique()`, solo se modifica la UX para no requerir ingreso manual.
